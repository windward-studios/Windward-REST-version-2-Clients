/**
 * @param applicationName 应用名称
 * @param datePattern 时间格式化
 * @param zippedArchive 旧日志压缩
 * @param maxSize 创建新文件之前,日志文件的最大大小
 * @param maxFiles 限制超出日志文件大小时创建的文件数
 * @param formatter 格式化输出,暂未实现
 * @param console 是否打印到控制台
 */
interface Log4jConfig {
    applicationName: string;
    datePattern?: string | null;
    zippedArchive?: boolean | null;
    maxSize?: string | null;
    maxFiles?: string | null;
    formatter?: Function | null;
    console?: boolean | null;
}
declare class Log4j {
    private options;
    constructor(options: Log4jConfig);
    /**
     *
     * @returns 返回 winston logger实例
     *
     * ```
     * const logger = new Log4j({ applicationName: 'test' }).getLogger();
     * logger.info('test');
     * ```
     */
    getLogger(): any;
}
export default Log4j;
